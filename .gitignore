node_modules/
dist/
*.log
.DS_Store
.env
.env.local

# 舊 dist 手動備份
dist.bak-*/
# codex 派工完成旗標
.codexdone_*
# 一次性測試輸出
mcp-test-out.txt
e2e-out.txt
# direct-api 的 session 檔（src/agents/direct-api.ts 寫到 workFolder/.tmp/api_sessions）。
# 派工時把 workFolder 指到本 repo 就會出現，而檔案裡是完整對話內容——
# 不該有機會被 commit 進來。
.tmp/
